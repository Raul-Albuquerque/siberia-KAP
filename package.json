{
  "name": "kap",
  "version": "1.0.0",
  "description": "Korean App",
  "scripts": {
    "dev": "parcel src/index.html src/styles/*.scss src/scripts/main.js --no-cache",
    "build": "parcel build src/index.html --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Raul Albuquerque",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0"
  }
}
